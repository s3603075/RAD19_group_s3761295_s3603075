
  <% User.all.each do |user| %>
  <div class="media">
    <div class="align-self-start mr-3">
      <%= gravatar_for user %>
    </div>
    <div class="media-body">
      <h4 class="mt-0"><%= user.name %></h4>
    </div>
  </div>
  <% end %>
